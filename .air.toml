root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./kanban-ui ."
  bin = "./kanban-ui"
  include_ext = ["go"]
  exclude_dir = ["ui", "tmp"]
  delay = 500
  stop_on_error = true
  kill_delay = "2s"
  send_interrupt = true

[misc]
  clean_on_exit = true
